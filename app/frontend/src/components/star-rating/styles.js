import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  starContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
  starButton: {
    paddingHorizontal: 2,
  },
});

export default styles;
